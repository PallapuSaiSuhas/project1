name: Go
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set-up Go
        uses: actions/setup-go@v5
        with: 
          go-version: '1.20'
      - name: Display Go version
        run: go version
